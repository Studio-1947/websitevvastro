# Sundargaan field recordings

Drop the recorded song files here (MP3 preferred), named to match the `src`
paths in `src/content/work/sundargaan.json` — e.g. `khachar-bhitor-achin-pakhi.mp3`.

Until a file exists, the case-study player shows that track as "arriving soon"
instead of erroring.
